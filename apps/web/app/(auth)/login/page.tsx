import { Auth } from "@/components/auth/login";

export default function SignUpPage() {
    return <Auth />
}

/* 
    Agency side
    We gonna take agency thing email and password, only email and password + google signup as well
    and inside the OS we have option to invite team members
*/